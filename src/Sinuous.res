// @bs.module("sinuous") external h: (string, list<string>, list<string>) => string = ""
@bs.module("sinuous") external observable: 'a => 'a = ""

// Sinuous/observable
// import { observable, computed, subscribe } from 'sinuous/observable';
